var app;
(function (app) {
    var toolbox;
    (function (toolbox) {
        'use strict';
        angular.module('app.toolbox', []);
    })(toolbox = app.toolbox || (app.toolbox = {}));
})(app || (app = {}));
